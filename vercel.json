{
"buildCommand": "npm --prefix apps/web run build",
  "outputDirectory": "apps/web/.vercel/output",
  "functions": {
    "_render": {
      "runtime": "nodejs20.x",
      "maxDuration": 30
    }
   }
}
